{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "npm run db:generate && (if [ \"$VERCEL_ENV\" = production ]; then npm run -w @cafeos/db deploy:push; fi) && npm run build",
  "outputDirectory": "apps/web/.next"
}
