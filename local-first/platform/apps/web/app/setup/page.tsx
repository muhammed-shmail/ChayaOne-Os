import SetupClient from './SetupClient';

export const metadata = {
  title: 'Setup — ChayaOne OS',
  description: 'First-time cafe setup wizard for ChayaOne OS Main PC',
};

export default function SetupPage() {
  return <SetupClient />;
}
