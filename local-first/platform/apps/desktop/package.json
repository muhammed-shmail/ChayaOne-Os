{
  "name": "@cafeos/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "tsc",
    "build": "tsc",
    "start": "electron .",
    "prepare-bundle": "node scripts/prepare-bundle.js",
    "build:installer": "npm run build -w @cafeos/web && node scripts/prepare-bundle.js && tsc && electron-builder --config electron-builder.yml",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "package": "npm run build -w @cafeos/web && node scripts/prepare-bundle.js && tsc && electron-builder --config electron-builder.yml --dir",
    "make": "electron-builder --config electron-builder.yml"
  },
  "dependencies": {
    "electron-log": "^5.0.0",
    "express": "^4.19.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "electron": "29.1.0",
    "electron-builder": "^26.15.3",
    "typescript": "^5.6.0"
  }
}
